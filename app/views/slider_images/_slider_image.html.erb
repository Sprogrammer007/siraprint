<div class="slide_item">
  <%= image_tag(slider_image.slide_image.url()) if slider_image.slide_image? %>
  <div class="slide_actions">
    <%= link_to "Remove", slider_image, :confirm => 'Are you sure?', :method => :delete %>
  </div>
</div>